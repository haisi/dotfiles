---
formula:
  ack:
  awscli:
  cloc: # Terminal: lines of code counter
  git:
  gnu-sed:
  htop:
  httpie:
  jq:
  shellcheck:
  sdkman-cli: # requires https://github.com/sdkman/homebrew-tap
  ssh-copy-id:
  tree: # Terminal: Shows tree structure of directory
  watch:
  wget:
  zsh-autosuggestions:
  zsh:
