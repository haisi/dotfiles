---
formula:
  ack:
  cloc: # Terminal: lines of code counter
  git:
  gnu-sed:
  htop:
  httpie:
  jq:
  shellcheck:
  ssh-copy-id:
  tree: # Terminal: Shows tree structure of directory
  watch:
  wget:
  zsh-autosuggestions:
  zsh:
