---
formula:
  ack:
  awscli:
  cloc: # Terminal: lines of code counter
  fd: # Better alternative to "find" command
  git:
  gnu-sed:
  htop:
  httpie:
  jq:
  neovim:
  openjdk:
  ripgrep:
  shellcheck:
  ssh-copy-id:
  terraform:
  tree: # Terminal: Shows tree structure of directory
  watch:
  wget:
  zsh-autosuggestions:
  zsh:
