---
formula:
  ack:
  awscli:
  cloc: # Terminal: lines of code counter
  git:
  gnu-sed:
  htop:
  httpie:
  jq:
  openjdk:
  shellcheck:
  ssh-copy-id:
  terraform:
  tree: # Terminal: Shows tree structure of directory
  watch:
  wget:
  zsh-autosuggestions:
  zsh:
